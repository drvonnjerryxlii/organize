<%= render partial: 'users', locals: { users: @admins, type: "admin" } %>

<%= render partial: 'users', locals: { users: @volunteers, type: "volunteer" } %>

<%= render partial: 'users', locals: { users: @students, type: "student" } %>
